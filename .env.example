# Variáveis de Ambiente - EXEMPLO
# Copie este arquivo para .env e configure seus valores

# Token de autenticação da API (use um token seguro e aleatório)
# Gere um token forte com: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_TOKEN=seu_token_secreto_super_seguro_aqui

# Porta padrão (8000 para local, deixar em branco ou 8000 para Vercel)
PORT=8000
